Passing props up and down can cause prop-drilling.
Hooks help us to get into componnet lifecycle.
Context helps us to put the state in context. We can have multiple contexts also. Context is a kind of cloud that hovers over aour app and we immidiately have access to any part of state. So prop-drilling is gone.

Context Api - Aletrnative to redux. But redux is needed for large size app.